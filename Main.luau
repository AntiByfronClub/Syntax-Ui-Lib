-- Canonical entry point for Studio and external environments.
-- The implementation is kept in `Main (1).luau` until the duplicate files
-- can be renamed in the repository UI.
return require(script.Parent:WaitForChild("Main (1)"))
